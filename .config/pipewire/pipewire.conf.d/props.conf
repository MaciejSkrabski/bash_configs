context.properties = {
    default.clock.allowed-rates = [ 44100, 48000 ]
    default.clock.quantum = 2048
    default.clock.min-quantum = 64
    default.clock.max-quantum= 4096

    default.video.width = 1600
    default.video.height = 900
}

# ensure converters accept 24/32 bit
#spa.properties = {
#    audioconvert.format = "S24_LE"
#    #audioconvert.format = "S24_SE"
#}
